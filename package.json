{
  "name": "codex-webui",
  "version": "0.2.0",
  "private": true,
  "description": "A simple web UI that proxies local Codex CLI sessions.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
